// var added_comment = $("<%#= render 'comments/comment', comment: @comment %>"); need to escape javascript

// var added_comment = $("<%= escape_javascript(render 'comments/comment', comment: @comment) %>");

// var added_comment = $("<%= j(render 'comments/comment', comment: @comment) %>"); // escape_javascript() => j()

var added_comment = $("<%= j(render @comment) %>"); // ultra shortcut 

$("#<%= dom_id(@comment.photo) %>_new_comment_form").before(added_comment);

$("#<%= dom_id(@comment.photo) %>_new_comment_form #comment_body").val(""); // clear form
